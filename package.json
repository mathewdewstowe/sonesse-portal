{
  "name": "sonesse-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx eleventy",
    "start": "npx eleventy --serve --port 8788",
    "dev": "npx eleventy --serve --port 8788",
    "deploy": "npx eleventy && npx wrangler pages deploy public --project-name=sonesse-website"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "wrangler": "^3.99.0"
  }
}
